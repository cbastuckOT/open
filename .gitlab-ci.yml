build:
  image: rikorose/gcc-cmake
  script:
    - echo "running scripts in the build job"
    - cmake -S . -B "build"
    - cmake --build ./build --config Release
  artifacts:
    paths:
      - build/app/EvalCI

pages:
  script:
    - cp -r web public
  artifacts:
    paths:
      - public